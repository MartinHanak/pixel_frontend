
export default function useGameAPI() {
    // game progress from save

    // request next question = general for current / next

    async function getNextQuestion() {
        //
    }

    // presenter comment for current

    // presenter current question and correct answer


    // load / request next question
    // loading status


    return {
        getNextQuestion
    }
}